# Privacy

- No location history upload by default.
- Saved places are stored only when the user creates them.
- Account deletion uses soft-delete initially and should be paired with retention/deletion policy before production.
- Passwords and tokens must never be logged.
- Refresh tokens are stored hashed.

