# Offline Format

Offline package foundation:

- `metadata.json`
- compact schedule database or binary package
- stops, stop areas, routes, trips, stop times, calendars, transfers and shapes
- search index
- validation summary
- version, checksum, validity and size metadata

The current implementation exposes package metadata and reserves binary/delta endpoints.

