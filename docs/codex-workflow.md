# Codex Workflow

Keep future Codex tasks narrow:

- one feature or subsystem per task
- include expected endpoint behavior and tests
- state whether database-backed behavior or fixture behavior is expected
- do not ask for frontend work unless the project scope changes explicitly
- review data-source licensing and production credentials separately

